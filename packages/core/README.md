# Core packages

Core packages provide vocabulary shared by multiple editor features. They are
kept separate from `packages/feature/` so feature packages can depend on the
editor model without owning it.
